x=int(input("enter the no of values for the list"))
for x in range(0,x):
   NUM=list(int(input(f"enter the {x}th list value")))
add=0
for n in NUM:
   add=add+n

print("the sum of values in list is"+add)
    